---
sidebar_position: 5
sidebar_label: "Stap 5: Tweede commit"
title: "Stap 5: een tweede commit"
---

import GitSimulator from '@site/src/components/GitSimulator';

# Stap 5: een tweede commit

Een project bestaat zelden uit één commit. Elke keer dat je iets verandert, herhaal je dezelfde drie stappen: aanpassen → `git add` → `git commit`.

In de simulator hieronder staat de eerste commit er al. Je kunt een bestand toevoegen of bewerken via de knoppen onder de zones. Voeg een tweede bestand toe, of pas `hello.txt` aan, en maak nog een commit.

<GitSimulator scenarioId="stap-5-tweede-commit" />

<details>
<summary>Klik hier voor een tip.</summary>

De eerste commit staat er al. Klik op **"+ nieuw bestand"** of **"bewerk hello.txt"** om iets te veranderen, en doe daarna `git add ...` en `git commit -m "..."`.

</details>
