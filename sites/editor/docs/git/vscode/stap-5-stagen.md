---
sidebar_position: 5
sidebar_label: "Stap 5: stagen"
title: "Stap 5: een bestand stagen (git add)"
hide_table_of_contents: true
---

# Stap 5: een bestand stagen (`git add`)

Zweef met je muis over `hello.txt` in de **Changes**-lijst. Er verschijnen drie knopjes. Klik op de **`+`** ("Stage Changes").

`hello.txt` verschuift naar **Staged Changes**. Dit is hetzelfde als `git add hello.txt` typen.

**Tip:** wil je alle bestanden in één keer stagen? Klik op de **`+`** naast de kop **Changes** zelf — dat is `git add .`.
