---
title: Wat is JavaScript?
description: HTML is de inhoud, CSS de vormgeving, JavaScript het gedrag - waarvoor je het gebruikt en hoe deze lessen werken.
---

# Wat is JavaScript?

Elke webpagina bestaat uit drie lagen. Vergelijk het met een huis:

| Taal | Rol | Vergelijking |
|---|---|---|
| **HTML** | De **inhoud** en structuur | De stenen, muren, het skelet |
| **CSS** | De **vormgeving** | De verf, het behang, de meubels |
| **JavaScript** | Het **gedrag** | De lichtschakelaars, de wifi, de deuren die opengaan |

Zonder JavaScript is een pagina statisch: alleen iets om te bekijken. Met JavaScript wordt de pagina interactief.

## Wat kan JavaScript?

Een paar voorbeelden van wat je in deze lessen bouwt:

- Een **knop** die de tekst op de pagina verandert zodra je hem klikt
- De **teller** die bijhoudt hoe vaak je iets hebt gedaan
- Een **verhaaltjesmachine** die vragen stelt en er een verhaal van maakt
- Het **score-systeem** dat reageert op wat de gebruiker doet

In het echte web doet JavaScript veel meer: video's afspelen, kaarten tonen, formulieren controleren, chatberichten versturen, spelletjes laten draaien. Alles wat *reageert* op wat jij doet, is bijna altijd JavaScript.

## Waar komt JavaScript te staan?

Net als CSS koppel je JavaScript aan een HTML-pagina. Dat kan op drie manieren:

1. **Inline**: direct in een HTML-attribuut zoals `onclick="..."`
2. **Intern**: in een `<script>`-tag binnenin je HTML
3. **Extern**: in een apart bestand `script.js`

Je leert ze in deze volgorde, van simpel naar professioneel.

## Hoe de lessen werken

Elke les volgt een vast patroon (de **PRIMM-methode**):

- **Predict**: eerst nadenken over wat de code gaat doen.
- **Run**: daarna pas uitvoeren in de editor.
- **Investigate**: experimenteren met kleine veranderingen.
- **Opdrachten**: drie per les, van klein aanpassen tot zelf bouwen, elk met een eigen oefenveld, tip en antwoord.

Elke les sluit af met **"Er gaat iets mis"**: de fout die je bij dit onderwerp in het echt tegenkomt, en hoe je hem herkent.

---

[Een knop die iets doet →](/docs/js-basics/inline-onclick)
