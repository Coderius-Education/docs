---
title: Beslissingen met if/else
description: Met if, else en else if laat je code anders reageren op verschillende situaties.
hide_table_of_contents: true
---

import CodeEditor from '@site/src/components/CodeEditor';

# Beslissingen met if/else

export const startHtml = `<!DOCTYPE html>
<html lang="nl">
<head>
  <meta charset="UTF-8">
  <title>If / else</title>
  <link rel="stylesheet" href="style.css">
  <script src="script.js" defer></script>
</head>
<body>
  <p id="uitvoer">Klik op de knop om je cijfer te zien.</p>
  <button onclick="bepaalCijfer()">Bepaal cijfer</button>
</body>
</html>`;

export const startCss = `p {
  font-size: 1.4rem;
  padding: 12px;
}

button {
  padding: 8px 16px;
  font-size: 1rem;
  cursor: pointer;
}`;

export const startJs = `function bepaalCijfer() {
  const score = Number(prompt('Wat is je score (0-100)?'));
  let cijfer;

  if (score >= 90) {
    cijfer = 'A';
  } else if (score >= 70) {
    cijfer = 'B';
  } else if (score >= 50) {
    cijfer = 'C';
  } else {
    cijfer = 'Onvoldoende';
  }

  document.getElementById('uitvoer').textContent = 'Score: ' + score + ' → ' + cijfer;
}`;

export const makeHtml = `<!DOCTYPE html>
<html lang="nl">
<head>
  <meta charset="UTF-8">
  <title>If / else</title>
  <link rel="stylesheet" href="style.css">
  <script src="script.js" defer></script>
</head>
<body>
  <p id="temp"></p>
  <p id="advies">Klik op de knop voor het advies.</p>
  <button onclick="geefAdvies()">Geef advies</button>
</body>
</html>`;

export const makeJs = `// Schrijf hier je code
`;

Tot nu toe deed je code altijd hetzelfde. Vaak wil je een andere actie afhankelijk van een waarde: "als de score boven 10 is, toon dan…". Dat schrijf je met een `if`-statement:

```js
if (score > 10) {
  document.getElementById('uitvoer').textContent = 'Gewonnen.';
}
```

Tussen de haakjes staat een **voorwaarde**: een expressie die `true` of `false` oplevert, precies wat je in [Vergelijken](/docs/js-basics/vergelijken) leerde. Is de voorwaarde `true`, dan voert JavaScript de regels tussen `{ ... }` uit; anders slaat hij het blok over.

## `else`: anders dit

Met `else` geef je het alternatief voor wanneer de voorwaarde **niet** waar is:

```js
if (score > 10) {
  document.getElementById('uitvoer').textContent = 'Gewonnen.';
} else {
  document.getElementById('uitvoer').textContent = 'Niet genoeg punten.';
}
```

Precies één van de twee blokken draait — nooit allebei.

## `else if`: meerdere opties

Voor meer dan twee gevallen zet je er `else if`-takken tussen:

```js
let cijfer;

if (score >= 90) {
  cijfer = 'A';
} else if (score >= 70) {
  cijfer = 'B';
} else if (score >= 50) {
  cijfer = 'C';
} else {
  cijfer = 'Onvoldoende';
}
```

JavaScript probeert de voorwaarden van boven naar beneden en stopt bij de eerste die `true` is. De regel `let cijfer;` maakt de variabele alvast aan zonder waarde; precies één van de takken vult hem daarna.

## Predict

Wat verschijnt er op de pagina als deze code draait?

```js
const leeftijd = 16;

if (leeftijd >= 18) {
  document.getElementById('uitvoer').textContent = 'Je mag stemmen.';
} else {
  document.getElementById('uitvoer').textContent = 'Nog even wachten.';
}
```

<details>
<summary>Welke van de twee zinnen wordt het?</summary>

`Nog even wachten.` De voorwaarde `16 >= 18` is `false`, dus JavaScript slaat het `if`-blok over en voert het `else`-blok uit.

</details>

## Probeer het zelf.

<CodeEditor
  initialHtml={startHtml}
  initialCss={startCss}
  initialJs={startJs}
  height="540px"
  livePreview={false}
/>

## Investigate

Experimenteer in het veld hierboven:

- Vul `95` in. En daarna `45`. Kloppen de cijfers met de grenzen in de code?
- Vul precies `50` in. Welke tak wint, en waarom die?
- Wissel de volgorde om: zet de tak met `score >= 50` bovenaan. Wat krijg je nu bij `95`? Verklaar het.

## Opdracht 1: Extra grens

Voeg een grens toe voor `score >= 30` met de waarde `'Bijna'`. Onder de 30 blijft het `'Onvoldoende'`.

<CodeEditor
  initialHtml={startHtml}
  initialCss={startCss}
  initialJs={startJs}
  height="540px"
  livePreview={false}
/>

<details>
<summary>Tip</summary>

Een extra `else if` tussen `score >= 50` en de laatste `else`. Houd de volgorde van hoog naar laag.

</details>

<details>
<summary>Antwoord</summary>

```js
if (score >= 90) {
  cijfer = 'A';
} else if (score >= 70) {
  cijfer = 'B';
} else if (score >= 50) {
  cijfer = 'C';
} else if (score >= 30) {
  cijfer = 'Bijna';
} else {
  cijfer = 'Onvoldoende';
}
```

</details>

## Opdracht 2: Quizvraag met feedback

Maak van de startcode een quizvraag: vraag met een prompt "Wat is de hoofdstad van Nederland?" en toon op de pagina "Goed." als het antwoord `amsterdam` is, en anders "Fout. Het juiste antwoord is Amsterdam."

<CodeEditor
  initialHtml={startHtml}
  initialCss={startCss}
  initialJs={startJs}
  height="540px"
  livePreview={false}
/>

<details>
<summary>Tip</summary>

Eén `if` met `===` en één `else`. Het antwoord is tekst, dus hier geen `Number()`.

</details>

<details>
<summary>Antwoord</summary>

```js
function bepaalCijfer() {
  const antwoord = prompt('Wat is de hoofdstad van Nederland?');

  if (antwoord === 'amsterdam') {
    document.getElementById('uitvoer').textContent = 'Goed.';
  } else {
    document.getElementById('uitvoer').textContent = 'Fout. Het juiste antwoord is Amsterdam.';
  }
}
```

Typt iemand `Amsterdam` met een hoofdletter, dan telt dat nu als fout — strings vergelijken is hoofdlettergevoelig (zie [Vergelijken](/docs/js-basics/vergelijken)).

</details>

## Opdracht 3: Weer-adviseur

Bouw een weer-adviseur. Vraag de temperatuur met een prompt en toon hem op de pagina, met daaronder het advies:

- warmer dan 25: "Drink genoeg water."
- 15 tot en met 25: "Lekker weer."
- 5 tot en met 14: "Trek een jas aan."
- onder de 5: "Sjaal en muts mee."

<CodeEditor
  initialHtml={makeHtml}
  initialCss={startCss}
  initialJs={makeJs}
  height="540px"
  livePreview={false}
/>

<details>
<summary>Tip</summary>

Vier takken, van warm naar koud. Als `temp > 25` al niet waar was, weet je bij de volgende tak dat de temperatuur hoogstens 25 is — je hoeft alleen nog de ondergrens te checken. De knop roept `geefAdvies()` aan.

</details>

<details>
<summary>Antwoord</summary>

```js
function geefAdvies() {
  const temp = Number(prompt('Hoeveel graden is het?'));
  let advies;

  if (temp > 25) {
    advies = 'Drink genoeg water.';
  } else if (temp >= 15) {
    advies = 'Lekker weer.';
  } else if (temp >= 5) {
    advies = 'Trek een jas aan.';
  } else {
    advies = 'Sjaal en muts mee.';
  }

  document.getElementById('temp').textContent = 'Temperatuur: ' + temp + ' graden';
  document.getElementById('advies').textContent = advies;
}
```

</details>

## Er gaat iets mis

**Toekenning (`=`) in plaats van vergelijking (`===`)**

```js
if (score = 10) {   // Fout: één =-teken
  // ...
}
```

Eén `=`-teken **kent toe** in plaats van **vergelijkt**. Deze regel zet `score` op `10` en geeft die `10` terug — en dat telt in JavaScript als `true`. Het `if`-blok draait dus altijd, wat de oorspronkelijke waarde ook was.

Schrijf voor een vergelijking altijd `===` (drie tekens). De editor of een linter waarschuwt vaak voor deze fout; neem die waarschuwing serieus.

---

[← Vergelijken](/docs/js-basics/vergelijken) | [Modern DOM →](/docs/js-basics/modern-dom)
