---
title: Een knop die iets doet
description: Je eerste JavaScript - een knop die met onclick en getElementById de pagina verandert.
---

import CodeEditor from '@site/src/components/CodeEditor';

# Een knop die iets doet

export const startHtml = `<!DOCTYPE html>
<html lang="nl">
<head>
  <meta charset="UTF-8">
  <title>Een knop die iets doet</title>
  <link rel="stylesheet" href="style.css">
</head>
<body>
  <p id="bericht">Nog niet geklikt.</p>
  <button onclick="document.getElementById('bericht').textContent = 'Je hebt geklikt.'">
    Klik mij
  </button>
</body>
</html>`;

export const startCss = `button {
  padding: 8px 16px;
  font-size: 1rem;
  cursor: pointer;
}

p {
  font-size: 1.2rem;
}`;

export const makeHtml = `<!DOCTYPE html>
<html lang="nl">
<head>
  <meta charset="UTF-8">
  <title>Een knop die iets doet</title>
  <link rel="stylesheet" href="style.css">
</head>
<body>
  <!-- Bouw hier je pagina -->
</body>
</html>`;

export const makeCss = `/* Schrijf hier je stijl */
`;

Dit is je eerste regel JavaScript, en hij staat rechtstreeks in de HTML:

```html
<p id="bericht">Nog niet geklikt.</p>
<button onclick="document.getElementById('bericht').textContent = 'Je hebt geklikt.'">
  Klik mij
</button>
```

Klik op de knop en de tekst in de `<p>` verandert. Er gebeuren twee dingen:

- **`onclick`** is een HTML-attribuut. De code die erin staat, voert de browser uit zodra je op het element klikt.
- **`document.getElementById('bericht')`** zoekt op de pagina het element met `id="bericht"`. Het `id` is het naamplaatje van een element; `getElementById` vindt het element dat dat naamplaatje draagt.

Het stukje `.textContent = '...'` erachter verandert de tekst van het gevonden element. In [de volgende les](/docs/js-basics/tekst-veranderen) kijk je daar preciezer naar; in deze les draait het om het **vinden** van het juiste element.

:::note[DOM]
De browser bewaart alle elementen van je pagina in een boomstructuur, de **DOM** (Document Object Model). `document` is de ingang van die boom, en `getElementById` is een van de manieren om er een element uit op te halen. Alles wat JavaScript op een pagina verandert, verandert het in de DOM.
:::

## Predict

Bekijk de startcode in de editor hieronder. Beantwoord de vraag **voordat** je op Run klikt.

<details>
<summary>Wat verandert er als je op de knop klikt, en wat verandert er niet?</summary>

De tekst in de `<p>` verandert van "Nog niet geklikt." naar "Je hebt geklikt.". De knop zelf verandert niet: die voert alleen de code in `onclick` uit.

</details>

## Probeer het zelf.

<CodeEditor
  initialHtml={startHtml}
  initialCss={startCss}
  height="420px"
  livePreview={false}
  stacked
/>

## Investigate

Experimenteer in het veld hierboven:

- Verander de tekst `'Je hebt geklikt.'` in iets anders. Wat zie je na een klik?
- Verander `id="bericht"` in de `<p>` naar `id="tekst"`, maar laat de `onclick` staan. Wat gebeurt er nu bij een klik?
- Geef twee `<p>`-elementen hetzelfde `id` en klik. Welke van de twee verandert er?

## Opdracht 1: Eigen teksten

Pas de startcode aan: de knop heet "Zeg hallo" en na een klik staat er "Hallo allemaal." in de `<p>`.

<CodeEditor
  initialHtml={startHtml}
  initialCss={startCss}
  height="420px"
  livePreview={false}
  stacked
/>

<details>
<summary>Tip</summary>

Er zijn twee teksten om aan te passen: de tekst tussen `<button>` en `</button>` (dat is wat op de knop staat) en de tekst achter `.textContent =` (dat is wat er na de klik verschijnt).

</details>

<details>
<summary>Antwoord</summary>

```html
<p id="bericht">Nog niet geklikt.</p>
<button onclick="document.getElementById('bericht').textContent = 'Hallo allemaal.'">
  Zeg hallo
</button>
```

</details>

## Opdracht 2: Twee elementen

Voeg een tweede `<p id="naam">` toe en een tweede knop die de tekst daarvan verandert naar jouw naam. De eerste knop moet blijven werken.

<CodeEditor
  initialHtml={startHtml}
  initialCss={startCss}
  height="420px"
  livePreview={false}
  stacked
/>

<details>
<summary>Tip</summary>

Elke `<p>` heeft een eigen `id` nodig. De nieuwe knop gebruikt `getElementById('naam')`, de oude blijft `getElementById('bericht')` gebruiken.

</details>

<details>
<summary>Antwoord</summary>

Toevoeging onder de bestaande knop:

```html
<p id="naam">Wat is jouw naam?</p>
<button onclick="document.getElementById('naam').textContent = 'Emma'">
  Toon naam
</button>
```

Twee elementen, twee naamplaatjes: elke knop wijst met zijn eigen `id` het juiste element aan.

</details>

## Opdracht 3: Quizvraag

Bouw vanaf een lege pagina een mini-quiz: een `<p>` met een vraag (bijvoorbeeld "Wat is de hoofdstad van Frankrijk?"), een lege `<p>` voor het antwoord, en een knop "Toon antwoord" die het antwoord in die tweede `<p>` zet.

<CodeEditor
  initialHtml={makeHtml}
  initialCss={makeCss}
  height="420px"
  livePreview={false}
  stacked
/>

<details>
<summary>Tip</summary>

Alleen de antwoord-`<p>` heeft een `id` nodig: dat is het enige element dat de knop moet kunnen vinden.

</details>

<details>
<summary>Antwoord</summary>

```html
<p>Wat is de hoofdstad van Frankrijk?</p>
<p id="antwoord"></p>
<button onclick="document.getElementById('antwoord').textContent = 'Parijs'">
  Toon antwoord
</button>
```

</details>

## Er gaat iets mis

**ID verkeerd gespeld**

```html
<p id="Bericht">Tekst</p>
<button onclick="document.getElementById('bericht').textContent = 'Hoi'">Klik</button>
```

`getElementById` is hoofdlettergevoelig: `'Bericht'` en `'bericht'` zijn twee verschillende id's. De browser vindt dan geen element, en er gebeurt niets — of je ziet een foutmelding in de browser-console (F12). Zorg dat het `id` in de HTML en in de `onclick` precies hetzelfde gespeld is.

---

[← Wat is JavaScript](/docs/js-basics/intro-javascript) | [Tekst veranderen →](/docs/js-basics/tekst-veranderen)
