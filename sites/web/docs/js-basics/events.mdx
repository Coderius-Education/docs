---
title: Events
description: Met addEventListener koppel je functies aan gebeurtenissen zoals klikken en muisbewegingen, los van de HTML.
---

import CodeEditor from '@site/src/components/CodeEditor';

# Events

export const startHtml = `<!DOCTYPE html>
<html lang="nl">
<head>
  <meta charset="UTF-8">
  <title>Events</title>
  <link rel="stylesheet" href="style.css">
  <script src="script.js" defer></script>
</head>
<body>
  <button id="knop">Beweeg je muis</button>
  <p id="status">Wachten op een event...</p>
  <p id="teller"></p>
</body>
</html>`;

export const startCss = `button {
  padding: 12px 24px;
  font-size: 1.1rem;
  cursor: pointer;
}

p {
  margin-top: 16px;
  font-size: 1.2rem;
}`;

export const startJs = `document.getElementById('knop').addEventListener('mouseover', function() {
  document.getElementById('status').textContent = 'Muis erop.';
});

document.getElementById('knop').addEventListener('mouseout', function() {
  document.getElementById('status').textContent = 'Muis weg.';
});`;

export const makeHtml = `<!DOCTYPE html>
<html lang="nl">
<head>
  <meta charset="UTF-8">
  <title>Events</title>
  <link rel="stylesheet" href="style.css">
  <script src="script.js" defer></script>
</head>
<body>
  <div class="kaart" id="kaart">
    <h2>Speelkaart</h2>
    <p>Beweeg eroverheen en klik erop.</p>
  </div>
  <p id="teller">Aantal klikken: 0</p>
</body>
</html>`;

export const makeCss = `.kaart {
  background-color: lightgray;
  padding: 20px;
  margin: 10px;
  border-radius: 8px;
  cursor: pointer;
}

.kaart.actief {
  background-color: gold;
}

p { font-size: 1.2rem; }`;

export const makeJs = `// Schrijf hier je code
`;

Tot nu toe koppelde je gedrag aan een knop met `onclick` in de HTML. Vanaf deze les doe je dat vanuit JavaScript, met **`addEventListener`** — dan blijven HTML en gedrag gescheiden, en kun je op veel meer reageren dan alleen klikken.

## Stap 1: een functie maken

Dit ken je al uit [De script-tag](/docs/js-basics/script-tag):

```js
function zegHoi() {
  document.getElementById('status').textContent = 'Geklikt.';
}
```

## Stap 2: de functie aan een event koppelen

`addEventListener` zet een luisteraar op een element. De drie onderdelen:

```js
document.getElementById('knop').addEventListener('click', zegHoi);
```

- `document.getElementById('knop')`: het element waarop je luistert
- `'click'`: de naam van het event waarop je reageert
- `zegHoi`: de functie die draait zodra het event plaatsvindt

De functie geef je mee **zonder haakjes**: je noemt zijn naam, zoals je iemand een telefoonnummer geeft. De browser "belt" hem pas bij het event.

## Korte vorm: anonieme functie

Gebruik je de functie maar op één plek, dan mag hij ook naamloos, direct in de listener. Dat heet een **anonieme functie**:

```js
document.getElementById('knop').addEventListener('click', function() {
  document.getElementById('status').textContent = 'Geklikt.';
});
```

Beide vormen doen precies hetzelfde; de anonieme versie is korter voor eenmalige acties.

## Welke events bestaan er?

| Event | Wanneer |
|-------|---------|
| `'click'` | Muisklik op het element |
| `'dblclick'` | Dubbelklik op het element |
| `'mouseover'` | Muis komt op het element |
| `'mouseout'` | Muis verlaat het element |
| `'keydown'` | Toets wordt ingedrukt |

:::tip[onclick of addEventListener?]
Voor één knop met één actie is `onclick` in de HTML prima — daarom gebruikten de vorige lessen hem. `addEventListener` kan meer: meerdere listeners op hetzelfde element, andere events dan klikken, en al je gedrag bij elkaar in script.js. Vanaf deze les is het de standaard.
:::

## Predict

Bekijk de startcode in het script.js-tabblad. Beantwoord de vraag **voordat** je op Run klikt.

<details>
<summary>Wat zie je als je de muis op de knop zet, en wat als je hem weer wegbeweegt?</summary>

Bij het raken van de knop zet het `mouseover`-event de tekst op "Muis erop."; zodra de muis de knop verlaat, zet het `mouseout`-event hem op "Muis weg.". Twee aparte listeners, elk voor hun eigen event.

</details>

## Probeer het zelf.

<CodeEditor
  initialHtml={startHtml}
  initialCss={startCss}
  initialJs={startJs}
  height="480px"
  livePreview={false}
/>

## Investigate

Experimenteer in het veld hierboven:

- Vervang `'mouseover'` door `'click'`. Wat verandert er aan het gedrag?
- Koppel twee `'click'`-listeners aan dezelfde knop, elk met een eigen `console.log`. Draaien ze allebei?
- Wat gebeurt er met de status-tekst als je klikt en daarna de muis wegbeweegt?

## Opdracht 1: Klik-listener erbij

Laat de bestaande listeners staan en voeg een derde toe: bij een `'click'` op de knop verschijnt "Geklikt." in de `<p id="teller">`.

<CodeEditor
  initialHtml={startHtml}
  initialCss={startCss}
  initialJs={startJs}
  height="480px"
  livePreview={false}
/>

<details>
<summary>Tip</summary>

Zelfde bouwplan als de twee listeners die er al staan, maar met `'click'` en het andere element. Zo zie je dat één knop meerdere listeners tegelijk kan hebben.

</details>

<details>
<summary>Antwoord</summary>

Toevoeging in script.js:

```js
document.getElementById('knop').addEventListener('click', function() {
  document.getElementById('teller').textContent = 'Geklikt.';
});
```

De muis-listeners blijven ook werken: elke listener reageert op zijn eigen event.

</details>

## Opdracht 2: Klik-teller

Breid je klik-listener uit tot een teller: bij elke klik komt er 1 bij, en de `<p id="teller">` toont "Aantal klikken: " met het aantal.

<CodeEditor
  initialHtml={startHtml}
  initialCss={startCss}
  initialJs={startJs}
  height="480px"
  livePreview={false}
/>

<details>
<summary>Tip</summary>

Net als in [Variabelen](/docs/js-basics/variabelen): declareer `let teller = 0;` **buiten** de listener, en verhoog hem binnenin.

</details>

<details>
<summary>Antwoord</summary>

```js
let teller = 0;

document.getElementById('knop').addEventListener('click', function() {
  teller = teller + 1;
  document.getElementById('teller').textContent = 'Aantal klikken: ' + teller;
});
```

</details>

## Opdracht 3: Speelkaart

Bouw het gedrag van de speelkaart: bij `mouseover` krijgt de kaart de klasse `actief` (die staat al in de CSS), bij `mouseout` raakt hij hem weer kwijt, en elke `click` telt mee in de `<p id="teller">`.

<CodeEditor
  initialHtml={makeHtml}
  initialCss={makeCss}
  initialJs={makeJs}
  height="480px"
  livePreview={false}
/>

<details>
<summary>Tip</summary>

Drie listeners op hetzelfde element (`getElementById('kaart')`). Voor de klasse gebruik je `classList.add` en `classList.remove` uit [Modern DOM](/docs/js-basics/modern-dom).

</details>

<details>
<summary>Antwoord</summary>

```js
let teller = 0;

document.getElementById('kaart').addEventListener('mouseover', function() {
  document.getElementById('kaart').classList.add('actief');
});

document.getElementById('kaart').addEventListener('mouseout', function() {
  document.getElementById('kaart').classList.remove('actief');
});

document.getElementById('kaart').addEventListener('click', function() {
  teller = teller + 1;
  document.getElementById('teller').textContent = 'Aantal klikken: ' + teller;
});
```

</details>

## Er gaat iets mis

**Haakjes achter de functienaam**

```js
function zegHoi() {
  document.getElementById('status').textContent = 'Hoi.';
}

document.getElementById('knop').addEventListener('click', zegHoi());  // Fout
```

Met `zegHoi()` (mét haakjes) roep je de functie **meteen** aan, al bij het laden van de pagina. Wat je aan `addEventListener` meegeeft is dan de *uitkomst* van die aanroep — niet de functie zelf. Gevolg: de tekst verandert direct bij het laden, en bij het klikken gebeurt er niets meer.

Geef de functie door **zonder haakjes**: `addEventListener('click', zegHoi)`.

---

[← Modern DOM](/docs/js-basics/modern-dom) | [Loops →](/docs/js-basics/loops)
