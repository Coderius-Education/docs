import HomepageFeatures from '@coderius/shared/components/HomepageFeatures';

// Geen feature-kaarten; opmaak + CSS staan in @coderius/shared.
export default function () {
  return <HomepageFeatures features={[]} />;
}
