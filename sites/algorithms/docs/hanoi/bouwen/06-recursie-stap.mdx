---
sidebar_position: 6
---

# Bouwsteen 3 — de recursie

<Voorkennis
  items={[
    {site: 'python', to: '/docs/data/10b-lijst-methoden', label: 'Lijst-methoden'},
  ]}
/>

**Leerdoel:** je zet de drie stappen om in code en maakt de functie
volledig recursief.

## De drie stappen

Pak het recept van [Op zoek naar patronen](../02-patronen#het-recept) er
nog eens bij. Om `n` schijven van `bron` naar `doel` te verplaatsen:

1. `n − 1` schijven naar de hulp-paal. Dat is hetzelfde probleem, met
   `doel` even als hulp: `hanoi(n - 1, bron, hulp, doel)`.
2. De grootste schijf naar doel, één losse zet: `(bron, doel)`.
3. `n − 1` schijven van hulp naar doel. Weer hetzelfde probleem, nu met
   `bron` als hulp: `hanoi(n - 1, hulp, doel, bron)`.

Kijk goed hoe de palen in stap 1 en 3 van rol wisselen: in elke deelstap is
een andere paal de tijdelijke parkeerplek. Die drie stukken plak je aan
elkaar tot één lijst zetten.

## Bouw en test

{/* niet-draaien: startcode; de leerling plakt de drie stukken aan elkaar en de eerste test faalt tot dan */}
<PyRunner initialCode={`def hanoi(n, bron, doel, hulp):
    if n == 0:
        return []

    zetten = []
    # 1. verplaats n-1 schijven van bron naar hulp (doel is even de hulp)
    #    zetten += hanoi(...)
    # 2. verplaats de grootste schijf van bron naar doel
    #    zetten.append(...)
    # 3. verplaats die n-1 schijven van hulp naar doel (bron is even de hulp)
    #    zetten += hanoi(...)
    return zetten


# === Tests ===
assert hanoi(1, "A", "C", "B") == [("A", "C")], "1 schijf: verwacht [('A', 'C')]"
assert hanoi(2, "A", "C", "B") == [("A", "B"), ("A", "C"), ("B", "C")], "2 schijven: verwacht A-B, A-C, B-C"
assert len(hanoi(3, "A", "C", "B")) == 7, "3 schijven: verwacht 7 zetten"
assert len(hanoi(6, "A", "C", "B")) == 63, "6 schijven: verwacht 63 zetten (2**6 - 1)"

print("Alle tests gehaald ✓")`} />

<details>
<summary>Antwoord</summary>

```python
def hanoi(n, bron, doel, hulp):
    if n == 0:
        return []

    zetten = []
    zetten += hanoi(n - 1, bron, hulp, doel)   # n-1 naar de hulp-paal
    zetten.append((bron, doel))                 # grootste schijf naar doel
    zetten += hanoi(n - 1, hulp, doel, bron)   # n-1 op hun plek
    return zetten
```

In stap 1 staat `hulp` op de plek van `doel`, want de schijven moeten nu
naar de hulp-paal, en `doel` doet dienst als tijdelijke hulp. In stap 3 is
het andersom. Verwissel je die twee, dan klopt de oplossing niet meer; zie
de [fouten-pagina](../10-fouten).

</details>

Door naar [compleet →](../07-compleet).
