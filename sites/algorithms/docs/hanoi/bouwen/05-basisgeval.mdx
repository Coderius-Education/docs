---
sidebar_position: 5
---

# Bouwsteen 2 — het basisgeval

<Voorkennis
  items={[
    {site: 'python', to: '/docs/beslissen/05b-if-else', label: 'If en else'},
  ]}
/>

**Leerdoel:** je begrijpt waarom recursie een stopconditie nodig heeft en
schrijft het basisgeval voor Hanoi.

## Waarom een basisgeval?

Straks roept `hanoi` zichzelf aan met steeds minder schijven: `n`, dan
`n − 1`, dan `n − 2`, enzovoort. Zonder afslag gaat dat door tot onder nul
en stopt het nooit. Python grijpt dan in met:

{/* foutmelding-van: def f(n):\n    return f(n - 1)\nf(3) */}
```
RecursionError: maximum recursion depth exceeded
```

Het **basisgeval** is die afslag: het allerkleinste probleem, dat je
direct kunt beantwoorden zonder jezelf nog eens aan te roepen.

## Wat is het kleinste geval?

Nog kleiner dan één schijf is nul schijven, en een lege stapel verplaatsen
kost geen enkele zet. Bij `n == 0` geef je dus een lege lijst `[]` terug.

Dat is handiger dan stoppen bij `n == 1`: straks telt de recursie vanzelf
af tot 0 en valt alles op zijn plek.

## Bouw en test

{/* niet-draaien: startcode; de leerling vult het basisgeval in en de assert faalt tot dan */}
<PyRunner initialCode={`def hanoi(n, bron, doel, hulp):
    # Het basisgeval: bij 0 schijven hoef je niets te doen.
    # Geef in dat geval een lege lijst terug.
    if n == 0:
        ...
    # (de rest komt op de volgende pagina)
    return [(bron, doel)]


# === Tests ===
assert hanoi(0, "A", "C", "B") == [], "0 schijven: verwacht een lege lijst"
assert hanoi(0, "B", "A", "C") == [], "0 schijven, andere palen: verwacht een lege lijst"

print("Basisgeval werkt ✓")`} />

<details>
<summary>Antwoord</summary>

```python
def hanoi(n, bron, doel, hulp):
    if n == 0:
        return []
    return [(bron, doel)]
```

`return` stopt de functie meteen, dus bij `n == 0` komt de regel eronder
niet meer aan bod. Die tijdelijke regel vervang je op de volgende pagina
door de echte recursie.

</details>

Door naar [de recursie →](./06-recursie-stap).
