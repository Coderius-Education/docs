---
sidebar_position: 6
---

# Bouwsteen 1 — `initial_state()`

**Leerdoel:** je schrijft de eenvoudigste functie van de track: een
**leeg bord** teruggeven.

## Wat doet deze functie?

`initial_state()` geeft het bord terug zoals het er aan het begin van
het spel uitziet: een 3×3 grid waar nog niets is gespeeld. Alle negen
cellen staan op `None`.

Deze functie heeft **geen input** — je hoeft nergens iets over te
vertellen, er is gewoon één antwoord.

## Specificatie

- **Input**: geen.
- **Output**: een lijst van 3 lijsten, elk met 3× `None`.
- **Voorbeeld**: `initial_state()` →
  ```python
  [
      [None, None, None],
      [None, None, None],
      [None, None, None],
  ]
  ```

## Voorspel

Als `initial_state()` correct werkt, wat denk je dat dit print?

{/* niet-draaien: voorspelling over de functie die de leerling hieronder bouwt */}
```python
bord = initial_state()
print(len(bord))
print(bord[1][1])
```

<details>
<summary>Antwoord</summary>

```
3
None
```

`len(bord)` is het aantal rijen (3). `bord[1][1]` is de middencel, en
die hoort leeg te zijn (`None`).

</details>

## Bouw zelf en test

{/* foutmelding-van: assert 1 == 2, "Bord moet 3 rijen hebben" */}

Vul de functie in. Onder de functie staan tests: regels die met
`assert` beginnen. Een `assert` controleert of wat erachter staat waar
is. Is dat niet zo, dan stopt het programma met een `AssertionError` en
de boodschap achter de komma, zodat je ziet wélke test faalde. Halen
alle tests het, dan zie je onderaan "Alle tests gehaald ✓". Zo werkt
het op elke bouwsteen-pagina.

{/* niet-draaien: startcode; de asserts falen tot de leerling de opdracht afmaakt */}
<PyRunner initialCode={`def initial_state():
    # vul aan: return een 3x3 lijst van None's
    return None


# === Tests ===
bord = initial_state()

assert bord is not None, "initial_state() returnt nog niets"
assert len(bord) == 3, "Bord moet 3 rijen hebben"
assert len(bord[0]) == 3, "Elke rij moet 3 cellen hebben"
for rij in bord:
    for cel in rij:
        assert cel is None, "Alle cellen moeten None zijn"

# Belangrijke test: rijen mogen niet hetzelfde object zijn (geen alias)
bord[0][0] = "X"
assert bord[1][0] is None, "Update aan rij 0 mag rij 1 niet beïnvloeden"

print("Alle tests gehaald ✓")`} />

<details>
<summary>Tip</summary>

Schrijf de drie rijen letterlijk uit, als een lijst met daarin drie
lijsten van drie keer `None`. Precies zoals het bord op
[het bord in Python](../04-bord-in-python) eruitzag, maar dan leeg.

⚠ Pas op met `[[None] * 3] * 3` — dat lijkt handig, maar geeft drie
keer dezélfde rij (alias). De laatste test hierboven vangt dat: een
wijziging in rij 0 zou anders ook rij 1 raken.

</details>

<details>
<summary>Antwoord</summary>

```python
def initial_state():
    return [
        [None, None, None],
        [None, None, None],
        [None, None, None],
    ]
```

In de startcode van latere bouwstenen staat de kortere schrijfwijze
`[[None] * 3 for _ in range(3)]`: die maakt óók drie losse rijen. Je
hoeft hem niet zelf te schrijven.

</details>

Door naar [bouwsteen 2: player →](./07-player).
